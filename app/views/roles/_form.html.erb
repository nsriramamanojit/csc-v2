<div class="form-container">
  <p class="legend">
    <strong>Note:</strong>
    Required fields are marked with an asterisk (<em>*</em>)
  </p>
  <%= simple_form_for(@role, :validate => true) do |f| %>
      <fieldset>
        <%= f.input :name,:placeholder=>"Enter Role Name" %>
        <%= f.input :description %>
        <%= f.input :reference_code, :label => 'Reference Name' %>
        <%= f.submit 'Save', :disable_with=>'Please wait...' %>
      </fieldset>
  <% end %>
</div>